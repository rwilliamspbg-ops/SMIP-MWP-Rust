# cli

Purpose: command-line binary (`mohawk-node`) entrypoints and helpers.

Build & run

```sh
cd cli
cargo run -- <args>
```

Notes

- The CLI exposes metrics flags used by stress and profiling harnesses.
